# NATIVE-T02-01 — execute the candidate in a provisioned review job

Problem recorded before implementation: the restored session has no callable KiCad, pcbnew or Arduino CLI; its ordinary package install fails under runtime permissions. Draft PR45 contains the candidate and validated native runner, but no repository workflow exists.

Implement a bounded Ubuntu 24.04 GitHub Actions review job on the existing draft branch. Use the official KiCad 9 release PPA, require KiCad/pcbnew 9.0.9 through the unchanged runner, and install Arduino CLI1.3.1 with the official release checksum, ESP32 core3.3.6 and NimBLE2.3.6. Run the exact PR head with read-only repository permission, no persisted checkout credentials, no secrets, no hardware ports, no deployment or merge. Upload logs and all runner artifacts even when checks fail. A setup or version mismatch must fail, not weaken a pin. Do not change repository settings or Actions permissions if access is rejected.

Acceptance is actual returned native outputs matched to candidate source and an inspected firmware build result, followed by correction of real findings. A workflow file, queued run or installed executable alone is not closure. This is a review-compute route within the authorized corrective work; the user's local machine is not modified.

Primary installation references: https://www.kicad.org/download/linux-distros/ ; https://github.com/arduino/arduino-cli/releases/tag/v1.3.1 . Exact checkout/upload action refs were resolved through their official repository Git refs before staging.

NATIVE-T02-02: The first actual run installed KiCad9.0.9 and verified Arduino CLI1.3.1, but ESP32's311MiB compiler download hit the CLI connection timeout at about87MiB. No CAD run occurred because setup was one serial step. Preserve run34376730854 and its logs. Split CAD provisioning/execution from firmware provisioning, increase the documented CLI connection timeout to1200s, and keep CAD findings available even if firmware setup fails. This changes download patience, not network permissions or version pins.

NATIVE-T02-03: Run34377213712 installed all pinned dependencies, but the workflow referenced runtime/run_native_candidate.py instead of the actual forwarded candidate/run_native_candidate.py. Correct both invocations and statically check the referenced Python/script paths against the213-file forwarded manifest and newly staged setup script. This was a workflow integration error; no board check ran in that failed attempt.

NATIVE-T02-04: Run34377626896 compiled the target firmware successfully and preserved valid ELF/application images. Native board load returned None; the wrapper then raised AttributeError at SetProject, hiding the primary load error. Use the native PCB_IO_MGR loader so its parser exception is retained, and run independent schematic ERC/netlist/BOM/PDF even if PCB fill fails. Preserve a separately labeled unfilled-input DRC diagnostic on a fill failure; it must not satisfy the final filled-board DRC gate. Re-run the existing no-output negative control. No CAD parse/fill pass is implied by this runner correction.
